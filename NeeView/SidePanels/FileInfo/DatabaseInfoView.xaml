<UserControl x:Class="NeeView.DatabaseInfoView" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NeeView"
             xmlns:properties="clr-namespace:NeeView.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300"
             Foreground="{DynamicResource Panel.Foreground}">
    
    <UserControl.Resources>
        <local:NullableToVisibilityConverter x:Key="NullableToVisibilityConverter" />
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="10">
            <!-- 标题显示 -->
            <StackPanel Visibility="{Binding HasTitle, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock Text="{Binding TitleLabel}" FontWeight="Bold" Margin="0,0,0,5" />
                <TextBlock Text="{Binding DisplayTitle}" 
                           Foreground="{Binding TitleColor}" 
                           FontSize="14" 
                           TextWrapping="Wrap" 
                           Margin="0,0,0,5" />
                <TextBlock Text="{Binding SecondaryTitle}" 
                           Foreground="{DynamicResource Panel.Foreground.Weak}"
                           FontSize="11" 
                           TextWrapping="Wrap" 
                           Margin="0,0,0,15"
                           Visibility="{Binding SecondaryTitle, Converter={StaticResource NullableToVisibilityConverter}}" />
            </StackPanel>

            <!-- Rating -->
            <TextBlock Text="评分:" FontWeight="Bold" Margin="0,0,0,5"
                       Visibility="{Binding HasRating, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <Border Background="{Binding RatingColor}" 
                    CornerRadius="3" 
                    Padding="8,4" 
                    HorizontalAlignment="Left"
                    Margin="0,0,0,15"
                    Visibility="{Binding HasRating, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock Text="{Binding RatingText}" 
                           Foreground="White" 
                           FontWeight="Bold" 
                           FontSize="16" />
            </Border>

            <!-- Tags -->
            <TextBlock Text="标签:" FontWeight="Bold" Margin="0,0,0,5"
                       Visibility="{Binding HasTags, Converter={StaticResource BooleanToVisibilityConverter}}" />
            
            <ItemsControl ItemsSource="{Binding TagGroups}" Margin="0,0,0,10"
                          Visibility="{Binding HasTags, Converter={StaticResource BooleanToVisibilityConverter}}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,0,0,10">
                            <TextBlock Text="{Binding GroupName}" 
                                       FontWeight="SemiBold" 
                                       Foreground="{DynamicResource Panel.Header}"
                                       Margin="0,0,0,3" />
                            <ItemsControl ItemsSource="{Binding Tags}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{DynamicResource Panel.Background.Alter}" 
                                                BorderBrush="{DynamicResource Panel.Border}" 
                                                BorderThickness="1" 
                                                CornerRadius="2" 
                                                Padding="6,2" 
                                                Margin="0,0,5,5">
                                            <TextBlock Text="{Binding}" 
                                                       FontSize="11" 
                                                       Foreground="{DynamicResource Panel.Foreground}" />
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- 无数据提示 -->
            <TextBlock Text="未找到数据库信息" 
                       Foreground="{DynamicResource Panel.Foreground.Weak}"
                       FontStyle="Italic"
                       Margin="0,10,0,0">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasAnyData}" Value="False">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </StackPanel>
    </ScrollViewer>
</UserControl>
