<UserControl x:Class="NeeView.FloatingControlPanelSidebarButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:properties="clr-namespace:NeeView.Properties"
             xmlns:local="clr-namespace:NeeView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <Style x:Key="PanelIconButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource IconButton}">
            <Setter Property="Width" Value="36" />
            <Setter Property="Height" Value="36" />
            <Setter Property="Margin" Value="2" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="ToolTip" Value="{properties:TextBinding FloatingControl.Title}" />
            <Setter Property="ToolTipService.Placement" Value="Left" />
        </Style>
    </UserControl.Resources>

    <Button x:Name="Root" Style="{StaticResource PanelIconButtonStyle}" Click="Root_Click">
        <Button.ContextMenu>
            <ContextMenu>
                <MenuItem Header="{properties:TextBinding FloatingControl.Settings}" Click="Settings_Click" />
            </ContextMenu>
        </Button.ContextMenu>
        <Grid Width="32" Height="32" Background="Transparent">
            <Grid x:Name="SelectedMark" Background="{DynamicResource Control.Accent}" Width="4" HorizontalAlignment="Right" Margin="-2,-2,0,-2" Visibility="{Binding IsExpanded, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <Path Width="20" Height="20" Fill="{DynamicResource Panel.Foreground}" Stretch="Uniform" 
                  Data="M12,2A10,10 0 0,0 2,12A10,10 0 0,0 12,22A10,10 0 0,0 22,12A10,10 0 0,0 12,2M12,4A8,8 0 0,1 20,12A8,8 0 0,1 12,20A8,8 0 0,1 4,12A8,8 0 0,1 12,4M11,7V13H13V7H11M11,15V17H13V15H11Z" />
        </Grid>
    </Button>
</UserControl>
