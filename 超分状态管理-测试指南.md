# 超分辨率状态管理系统重构 - 测试指南

## ✅ 重构完成

已完成**严格的三路独立控制系统**,彻底解决之前的状态冲突问题。

---

## 🎯 核心设计原则

### 1. **三个独立控制路径**
- **全局开关** (`Config.IsEnabled`): 总开关,关闭后所有超分停止
- **用户单张切换** (数据库 `UserPreference`): 用户对单张图片的明确意图
- **自动超分条件** (`AutoApplyOnView` + 尺寸/文件大小条件): 仅在 Auto 模式下生效

### 2. **决策优先级**
```
用户单张切换(Enabled/Disabled) > 全局开关 > 自动超分条件(Auto)
```

### 3. **状态分离**
- **用户偏好** (`UserPreference`): `Auto`(默认) / `Enabled`(强制启用) / `Disabled`(强制禁用)
- **实际状态** (`ActualStatus`): `None` / `Processing` / `Completed` / `Failed`
- **UI 显示**: 勾选框只反映用户偏好,不会自动修改

---

## 📊 数据库持久化

### 数据库位置
```
%LocalAppData%\NeeView\SuperResolutionState.db
```

### 表结构 `SuperResolutionState`
| 字段 | 类型 | 说明 |
|------|------|------|
| `ImagePath` | TEXT (主键) | 图片完整路径 |
| `UserPreference` | INTEGER | 0=Auto, 1=Enabled, 2=Disabled |
| `ActualStatus` | INTEGER | 0=None, 1=Processing, 2=Completed, 3=Failed |
| `OriginalWidth/Height` | INTEGER | 原图分辨率 |
| `SuperResolutionWidth/Height` | INTEGER | 超分后分辨率 |
| `CacheLocation` | TEXT | 缓存位置 |
| `LastModified` | INTEGER | 最后修改时间(Unix时间戳) |
| `CreatedTime` | INTEGER | 创建时间 |

---

## 🔬 测试场景

### 场景 1: 全局开关关闭
**操作**: 关闭"启用超分辨率"全局开关

**预期行为**:
- ✅ 所有新加载的图片**都不超分**(无论数据库中用户偏好如何)
- ✅ 已有缓存的超分图片切回原图
- ✅ 日志显示: `[跳过超分] xxx.png (用户已禁用)` 或 `[跳过超分] xxx.png (不符合自动条件)`

**验证方法**:
1. 关闭全局开关
2. 浏览任意图片
3. 检查日志文件: `%LocalAppData%\NeeView\Logs\SuperResolution_DEV_{日期}.log`
4. 确认没有 `[自动超分]` 或 `[强制超分]` 记录

---

### 场景 2: 用户禁用单张图片
**操作**: 
1. 全局开关**开启**
2. 勾选"当前图片启用超分"后,再次**取消勾选**

**预期行为**:
- ✅ 数据库记录 `UserPreference = Disabled`
- ✅ 图片切换到原图,不会再自动超分
- ✅ 日志显示: 
  ```
  [STATE] xxx.png | UserPreference: Auto → Disabled | Trigger=UserToggle
  [跳过超分] xxx.png (用户已禁用)
  ```
- ✅ 再次打开该图片,勾选框保持**未勾选**状态

**验证方法**:
1. 打开图片,取消勾选
2. 翻到其他图片,再翻回来
3. 确认勾选框仍然是未勾选
4. 查看日志确认 `UserPreference: Auto → Disabled`

---

### 场景 3: 用户强制启用单张图片
**操作**: 
1. 全局开关**开启**
2. 打开一张**不符合自动条件**的图片(如尺寸过大)
3. **手动勾选**"当前图片启用超分"

**预期行为**:
- ✅ 数据库记录 `UserPreference = Enabled`
- ✅ **强制执行超分**,忽略尺寸/文件大小限制
- ✅ 日志显示:
  ```
  [STATE] xxx.png | UserPreference: Auto → Enabled | Trigger=UserToggle
  [强制超分] xxx.png (用户已启用)
  [强制超分成功] xxx.png: 1920x1080 → 3840x2160
  ```
- ✅ 再次打开该图片,自动应用超分

**验证方法**:
1. 找一张大图(如 2000x2000),配置自动超分最大尺寸=1500
2. 打开该图,正常情况下不会自动超分
3. 手动勾选,确认强制执行超分
4. 查看日志确认 `[强制超分]`

---

### 场景 4: Auto 模式 - 符合自动条件
**操作**:
1. 全局开关**开启**
2. "查看时自动应用"**开启**
3. 打开一张**符合自动条件**的图片
4. 用户**未手动勾选/取消**

**预期行为**:
- ✅ 数据库 `UserPreference = Auto`(默认)
- ✅ 自动检查条件后执行超分
- ✅ 日志显示:
  ```
  [自动超分] xxx.png (128.5 KB, 800x600)
  [自动超分成功] xxx.png: 800x600 → 1600x1200
  [STATE] xxx.png | New record | Pref=Auto, Status=Completed | Trigger=AutoComplete
  ```

**验证方法**:
1. 配置自动超分条件(如最小宽度=500, 最大宽度=1500)
2. 打开符合条件的图片
3. 确认自动超分
4. 查看日志确认 `[自动超分]` 和 `Pref=Auto`

---

### 场景 5: Auto 模式 - 不符合自动条件
**操作**:
1. 全局开关**开启**
2. "查看时自动应用"**开启**
3. 打开一张**不符合自动条件**的图片(如尺寸过大)

**预期行为**:
- ✅ 数据库 `UserPreference = Auto`
- ✅ **不执行超分**,使用原图
- ✅ 日志显示:
  ```
  [跳过超分] xxx.png (不符合自动条件)
  ```

**验证方法**:
1. 配置自动超分最大尺寸=1500
2. 打开 2000x2000 的图片
3. 确认不超分
4. 查看日志确认 `[跳过超分]`

---

### 场景 6: 状态持久化
**操作**:
1. 对图片A: 手动勾选(Enabled)
2. 对图片B: 手动取消(Disabled)
3. 对图片C: 不操作(Auto)
4. **关闭 NeeView**
5. **重新启动 NeeView**
6. 重新浏览这3张图片

**预期行为**:
- ✅ 图片A: 勾选框**已勾选**,自动超分
- ✅ 图片B: 勾选框**未勾选**,不超分
- ✅ 图片C: 勾选框根据自动条件显示,按 Auto 逻辑处理

**验证方法**:
1. 按上述操作设置3张图片
2. 关闭程序
3. 用 SQLite 工具查看数据库:
   ```sql
   SELECT ImagePath, UserPreference, ActualStatus FROM SuperResolutionState;
   ```
4. 重启程序,浏览这3张图片确认状态正确

---

## 📝 日志解读

### 日志文件位置
```
%LocalAppData%\NeeView\Logs\SuperResolution_DEV_{YYYYMMDD}.log
```

### 关键日志标识

#### 状态变更日志
```
[2025-11-07 14:30:25.123] [STATE] image.png | UserPreference: Auto → Enabled | Trigger=UserToggle
[2025-11-07 14:30:25.456] [STATE] image.png | ActualStatus: None → Processing | Trigger=ForcedByUser
[2025-11-07 14:30:28.789] [STATE] image.png | ActualStatus: Processing → Completed | Trigger=ForcedComplete
```

#### 超分决策日志
```
[跳过超分] image.png (用户已禁用)           ← UserPreference=Disabled
[跳过超分] image.png (不符合自动条件)        ← Auto模式但不满足条件
[强制超分] image.png (用户已启用)           ← UserPreference=Enabled
[自动超分] image.png (128 KB, 800x600)      ← Auto模式且满足条件
```

#### 成功/失败日志
```
[强制超分成功] image.png: 800x600 → 1600x1200
[自动超分成功] image.png: 800x600 → 1600x1200
[强制超分失败] image.png, 使用原图
[自动超分异常] image.png: OutOfMemoryException
```

---

## 🐛 已知问题修复

### ❌ 之前的问题
1. **全局开关关闭仍超分**: 因为只检查了 `AutoApplyOnView`,没检查 `IsEnabled`
2. **勾选后又跳回原图**: `UpdateCurrentImageInfo` 自动修改 `EnableCurrentImageSuperResolution`,与用户操作冲突
3. **无法确认是否超分**: 缺少持久化状态记录

### ✅ 修复方案
1. `BitmapPictureSource` 严格检查 `IsEnabled` + 数据库用户偏好
2. `UpdateCurrentImageInfo` 只读取数据库,不修改用户偏好,使用 `_isUpdatingFromDatabase` 防止递归
3. 所有状态变更记录到 SQLite 数据库 + 详细日志

---

## 🔧 开发者调试

### 查看数据库
```bash
# 使用 SQLite Browser 或命令行
cd %LocalAppData%\NeeView
sqlite3 SuperResolutionState.db

# 查询所有记录
SELECT 
    substr(ImagePath, -30) as Image,
    CASE UserPreference 
        WHEN 0 THEN 'Auto'
        WHEN 1 THEN 'Enabled'
        WHEN 2 THEN 'Disabled'
    END as Preference,
    CASE ActualStatus
        WHEN 0 THEN 'None'
        WHEN 1 THEN 'Processing'
        WHEN 2 THEN 'Completed'
        WHEN 3 THEN 'Failed'
    END as Status,
    OriginalWidth || 'x' || OriginalHeight as Original,
    SuperResolutionWidth || 'x' || SuperResolutionHeight as SR,
    datetime(LastModified, 'unixepoch') as Modified
FROM SuperResolutionState
ORDER BY LastModified DESC
LIMIT 20;
```

### 清空状态测试
```sql
-- 清空所有用户偏好(恢复 Auto)
DELETE FROM SuperResolutionState;

-- 只清空某个图片的偏好
DELETE FROM SuperResolutionState WHERE ImagePath LIKE '%test.png%';
```

### 手动设置偏好
```sql
-- 强制启用某张图片
UPDATE SuperResolutionState 
SET UserPreference = 1  -- Enabled
WHERE ImagePath LIKE '%test.png%';

-- 强制禁用某张图片
UPDATE SuperResolutionState 
SET UserPreference = 2  -- Disabled
WHERE ImagePath LIKE '%test.png%';
```

---

## 📚 代码架构

### 新增文件
1. `SuperResolutionStateDatabase.cs` - SQLite 数据库操作
2. `SuperResolutionStateManager.cs` - 状态管理单例

### 修改文件
1. `SuperResolutionViewModel.cs` - 初始化数据库,从数据库读取状态
2. `BitmapPictureSource.cs` - 严格的三路决策逻辑
3. `SuperResolutionLogger.cs` - (已有)

### 状态流转
```
用户勾选/取消
    ↓
EnableCurrentImageSuperResolution.set
    ↓
保存到数据库 (UserPreference = Enabled/Disabled)
    ↓
触发 HandleCurrentImageToggleAsync
    ↓
Unload/ReLoad 页面
    ↓
BitmapPictureSource.CreateImageSourceAsync
    ↓
查询数据库 UserPreference
    ↓
分支决策:
  - Disabled → 跳过超分
  - Enabled → 强制超分
  - Auto + IsEnabled + AutoApplyOnView + 符合条件 → 自动超分
    ↓
更新数据库 ActualStatus
    ↓
记录日志
```

---

## 🎉 测试检查清单

- [ ] 场景1: 全局开关关闭 → 所有图不超分
- [ ] 场景2: 用户禁用单张 → 该图不超分,状态持久化
- [ ] 场景3: 用户强制启用 → 忽略条件强制超分
- [ ] 场景4: Auto模式符合条件 → 自动超分
- [ ] 场景5: Auto模式不符合条件 → 不超分
- [ ] 场景6: 重启程序 → 状态正确恢复
- [ ] 日志记录完整清晰
- [ ] 数据库正确存储状态

---

## 💡 提示

1. **首次运行**: 所有图片默认 `UserPreference = Auto`,会根据自动条件判断
2. **清除历史状态**: 删除 `SuperResolutionState.db` 文件可重置所有偏好
3. **性能**: 数据库使用 WAL 模式,支持并发读写,性能良好
4. **日志**: Debug 模式会记录所有状态变更,Release 模式仅记录 Info 及以上

---

**祝测试顺利!如有问题请查看日志文件。** 🚀
